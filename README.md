# Raspberry Pi Temperature Monitor (rpi-monitor-temp)

Basic (for now) script to monitor temps on the PI CLI, written in Python

## Install

Just:
`pip3 install -r requirements.txt`

## Run

Just:
`$ python ./monitor-temp.py`
